import React from 'react';
import './Header.css'

export default function Header() {
    return (
        <header>
            <h1 className="header-title">Manga Display</h1>
        </header>
    );
}